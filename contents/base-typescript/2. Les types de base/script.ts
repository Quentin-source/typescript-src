let str = "str"

let num = 10;

let array = [];

let obj = {};

let toggle = true;

let anything; //on peut mettre ce qu'on veut

let randomNumber : number;

const conversion = (celsius : number) => {
    return (celsius * 9/5) + 32;
}
console.log(conversion(10));