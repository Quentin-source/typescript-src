// Tuple
// Avec une virgule un crée un tupple c'est un schèma exact pour un tableau
let tuple: [boolean, number];
tuple = [true, 25];

// Enum ??

const Roles = {
  JAVASCRIPT: 1,
  CSS: 2,
  REACT: 3,
};

console.log(Roles);

enum admin {
  JAVASCRIPT = 1,
  CSS,
  REACT,
}

console.log(admin);
